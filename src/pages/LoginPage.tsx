import GoogleAuthButton from '../components/LoginPage/GoogleAuthButton';

export default function LoginPage() {
  return (
    <div className='flex min-h-screen w-full flex-col items-center justify-center'>
      <GoogleAuthButton />
    </div>
  );
}
